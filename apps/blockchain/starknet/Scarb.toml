[package]
name = "starklane"
version = "0.1.0"

[dependencies]
starknet = ">=2.1.0"
#snforge_std = { path = "../../../../../starknet/starknet-foundry/snforge_std/" }

[[target.starknet-contract]]
sierra = true
casm = true

[lib]
